== myCOLLEGES

To set up:

1) Fork the repo

2) Install the gemfile with "bundle install" in your terminal.

3) Install node modules with "npm install".

4) Start a server with "rails s".

5) In your browser navigate to localhost:3000.
